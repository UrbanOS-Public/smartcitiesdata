# discovery_streams Release Notes

## 3.0.25 (pending release — not yet tagged)
- Added support for multiple datasets per ingestion
- Enabled XML request bodies for HTTP POST streaming
- Updated the Alpine base image and dependencies
- General logging improvements
