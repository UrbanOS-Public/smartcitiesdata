defmodule ProtocolDestination.IntegrationTest do
  use ExUnit.Case, async: true
  describe "Protocol Destination High Level Tests" do
    test "all modules are properly loaded" do
      assert true
    end
  end
end