defmodule Andi.DowncaseMigration do
  alias Andi.SchemaDowncaser

  # @instance Andi.instance_name()

  def do_migration() do
    # Brook.get_all_values!(:dataset)
    # Brook.Event.send(@instance, type, author, event, driver)
    # SchemaDowncaser.downcase_schema(schema)
    # get all datasets
    # downcase each dataset
    # merge that dataset back to state
    # send the dataset update
  end
end
